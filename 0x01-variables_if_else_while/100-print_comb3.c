#include <stdio.h>
#include <stdlib.h>
/**
 * main - Print all possible different combinations of two digits.
 *
 * Return: Always 0.
 */
int main(void)
{
        int a = 48;
        int b = 57;
        int c; 
        
        for (a = 48; a < c ; a++)
        {
            putchar(a);
            
        for (b = 57; b < c; b++)
        
        {
            putchar(c);
        }
        
        if ( a < c && b < c, ++c) 
        continue; 
            
            putchar(',');
            putchar(' ');
        }
        putchar('\n');
        return (0);

}
