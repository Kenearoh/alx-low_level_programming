#include <stdio.h>
/**
 * main - Print all single digit numbers of base 10 starting from 0
 *
 * Return: Always 0.
 */
int main(void)
{
	int b;

	for (b = 0; b <= 9; b++)
		printf("%d", b);
	return (0);
}
