#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b = 97;

	while (b <= 122)
	{
		putchar(b);
			b++;
	}
	putchar('\n');
	return (0);
}
